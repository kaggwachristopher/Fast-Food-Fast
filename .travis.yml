language: python
python:
  - "3.6"
install:
  - pip install pytest-cov
  - pip install coveralls
  - pip install -r requirements.txt
script: 
  - pytest test --cov=app/app --cov-report term-missing

after_success:
  - coveralls